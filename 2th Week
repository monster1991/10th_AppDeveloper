2주차 총정리 220313
1. Expo 설치, Visual Studio Code에서 Expo 실행하기
1-1. Win10에서 터미널은 PowerShell로 실행되는데 바로 expo start 시 에러 발생, 명령프롬포트로 진행하면 Tunnel 오류.
1-2. PowerShell 터미널에서 Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope Process 하여 권한획득후 진행
1-3. 다음 Visual Studio Code 실행 후 꼭 해야함.
2. 회사에서 집 컴퓨터를 원격하여 진행하고 있으므로 expo start - Tunnel로 진행

3. App.js에서 코딩
4. App() 함수에서 골격 및 함수, 변수 선언

5. StyleSheet.create로 각 태그 옷입히기
6. Flex는 같은 구역내 비율로 구역 나누기
7. flexDirection = 영역내 구역 정렬 방향 (row-가로, column 세로)
8. justifyContent = 영역내 Contents(text 등) 정렬 방향(flex-start 영역 시작줄, center 가운데줄, flex-end 영역 마지막줄)
9. alignItem = 줄에서 컨텐츠가 정렬되는 방향

10. Text 태크 numberOfLine 속성 - 지정된 줄 이상 넘어갈 경우 말줄임표... 표시

11. Dictionary로 구성된 data.json의 데이터를 받아와 map을 이용한 반복출력
12. x > y ? a : b 삼항연산자를 이용해 반복문 다르게 출력하기

13. 2주차 숙제(about.js) 코드
import React from 'react';
import { StyleSheet, Text, View, Image, TouchableOpacity, ScrollView, Linking} from 'react-native';

const man='https://firebasestorage.googleapis.com/v0/b/sparta-image.appspot.com/o/lecture%2FaboutImage.png?alt=media&token=13e1c4f6-b802-4975-9773-e305fc7475c4'
const josungInstargram='https://www.instagram.com/joesungji/'

export default function AboutPage() {

    const onPress = () => {
        return(Linking.openURL(josungInstargram))
    }

  return (
      <ScrollView style={styles.container}>
          <View>
            <Text style={styles.containerTopText}>HI! 스파르타코딩 앱개발 반에 오신것을 환영합니다</Text>
          </View>
          <View style={styles.containerBottom}>
            <Image style={styles.manImage} source={{uri:man}}/>
            <Text style={styles.containerBottomText01}>많은 내용을 간결하게 담아내려 노력했습니다!</Text>
            <Text style={styles.containerBottomText02}>꼭 완주하셔서 꼭 여러분 것으로 만들어가시길 바랍니다</Text>
            <TouchableOpacity style={styles.containerBottomButton}><Text style={styles.containerBottomButtonText} onPress={onPress}>조성's 인스타그램</Text></TouchableOpacity>
          </View>
      </ScrollView>
  )

}

const styles = StyleSheet.create({
    container:{
        flex:1,
        backgroundColor:"#BB00BB"
    },
    containerTop:{
        flex:1
    },
    containerTopText:{
        color:"#fff",
        fontSize:35,
        fontWeight:"700",
        marginTop:50,
        marginLeft:25,
    },
    containerBottom:{
        flex:4,
        backgroundColor:"#fff",
        borderRadius:10,
        margin:20,
        marginBottom:20,
    },
    manImage:{
        width:200,
        height:200,
        borderRadius:30,
        marginTop:100,
        alignSelf:"center"
    },
    containerBottomText01:{
      margin:25,
      fontSize:25,
      fontWeight:"700",
      textAlign:"center"
    },
    containerBottomText02:{
      margin:25,
      fontSize:20,
      fontWeight:"700",
      textAlign:"center"
    },
    containerBottomButton:{
      width:200,
      height:60,
      padding:20,
      backgroundColor:"#fdc453",
      borderColor:"deeppink",
      borderRadius:15,
      margin:7,
      marginBottom:50,
      alignSelf:"center"
    },
    containerBottomButtonText:{
        color:"#fff",
        textAlign:"center",
        fontWeight:"700"
    }
})
